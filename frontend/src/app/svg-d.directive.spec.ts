import { SvgDDirective } from './svg-d.directive';

describe('SvgDDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgDDirective();
    expect(directive).toBeTruthy();
  });
});
